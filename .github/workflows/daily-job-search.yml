name: Career Agent

on:
  schedule:
    - cron: '17 1 * * *' # Runs at ~6:47 AM IST (1:17 AM UTC) to avoid GitHub Actions scheduling delays at the half-hour mark
  workflow_dispatch:

jobs:
  run-job-search:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Run Career Agent
        run: npm run dev
        env:
          JSEARCH_API_KEY: ${{ secrets.JSEARCH_API_KEY }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}